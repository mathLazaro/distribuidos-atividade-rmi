{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Server: MainServer",
      "request": "launch",
      "mainClass": "distribuidos.rmi.server.MainServer",
      "classPaths": [
        "${workspaceFolder:server}/target",
        "${workspaceFolder:shared}/target/shared.jar"
      ],
      "cwd": "${workspaceFolder:server}",
      "console": "integratedTerminal"
    }
  ]
}
