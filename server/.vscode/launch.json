{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Server: MainServer",
      "request": "launch",
      "mainClass": "distribuidos.rmi.MainServer",
      "classPaths": [
        "${workspaceFolder}/target",
        "${workspaceFolder}/../shared/target/shared.jar"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
